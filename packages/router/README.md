# @fluxdom/router

Experimental route matching and browser navigation primitives for FluxDOM.
`matchRoute` is environment-independent; `initRouter` and `navigate` require a
browser. Component mounting, nested routes, and data loading are not implemented.
