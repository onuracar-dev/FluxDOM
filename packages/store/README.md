# @fluxdom/store

Experimental signal-backed stores for FluxDOM. Stores expose getter functions,
generated setters, optional actions, and best-effort local/session persistence.
Persistence is browser-only and does not validate or encrypt data.
