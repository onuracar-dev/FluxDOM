{
  "name": "FluxDOM-monorepo",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "tsc -b tsconfig.base.json && tsc -b packages/vite-plugin/tsconfig.json",
    "dev": "npm run dev --workspaces",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,js,json,md,html,css}\"",
    "publish-all": "npm publish --workspaces --access public"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ]
}
